# Reproducibility Check — C-MAPSS Pipeline
Date: 2026-05-08  
Purpose: Verify all thesis table values reproduce from current codebase before starting FEMTO phase.

---

## Overall Result: ALL PASS

No thesis value deviates beyond the stated tolerance. One path bug was found and fixed in `06_shap_analysis.py`.

---

## GÖREV 1 — Cluster Pipeline (Table 4.8)
**Script:** `src/cmapss/03b_cluster_pipeline.py`  
**Threshold:** ±0.5 cycle

| Subset | Metric | Thesis | Reproduced | Diff | Status |
|--------|--------|--------|------------|------|--------|
| FD001 | Pseudo RMSE | 32.05 | 32.05 | +0.00 | PASS |
| FD001 | True RMSE | 21.15 | 21.15 | +0.00 | PASS |
| FD002 | Pseudo RMSE | 42.40 | 42.40 | +0.00 | PASS |
| FD002 | True RMSE | 31.89 | 31.89 | +0.00 | PASS |
| FD003 | Pseudo RMSE | 28.87 | 28.87 | +0.00 | PASS |
| FD003 | True RMSE | 22.50 | 22.50 | +0.00 | PASS |
| FD004 | Pseudo RMSE | 42.03 | 42.03 | +0.00 | PASS |
| FD004 | True RMSE | 33.69 | 33.69 | +0.00 | PASS |

Max deviation across all metrics: 0.02% (floating-point rounding only).  
Pipeline's own canonical sanity check: **ALL PASS** (tol=1%).

---

## GÖREV 2 — SHAP Analysis (Table 4.6)
**Script:** `src/cmapss/06_shap_analysis.py`  
**Threshold:** ±5%

| Metric | Thesis | Reproduced | Diff | Diff% | Status |
|--------|--------|------------|------|-------|--------|
| anomaly_score pseudo mean\|SHAP\| | 11.582 | 11.5822 | +0.0002 | 0.002% | PASS |
| anomaly_score true mean\|SHAP\| | 3.228 | 3.2284 | +0.0004 | 0.012% | PASS |
| sensor_11 true mean\|SHAP\| (rank 1) | 4.444 | 4.4442 | +0.0002 | 0.005% | PASS |

anomaly_score is #1 pseudo feature in all 4 subsets: **PASS**  
sensor_11 is #1 true feature for FD001: **PASS**

**Bug fixed:** `_SRC = ROOT / "src"` resolved to `src/src/` after folder restructure.  
Fixed to `_SRC = Path(__file__).parent`. Script now runs correctly.

---

## GÖREV 3 — Bias Calibration (Table 4.13)
**Script:** `src/cmapss/08_bias_calibration.py`  
**Threshold:** ±0.5 cycle  
Note: Reads `cluster_optimal_FDXXX_full.csv` generated by 06_shap_analysis.py.

| Subset | Metric | Thesis | Reproduced | Diff | Status |
|--------|--------|--------|------------|------|--------|
| FD001 | RMSE Before | 32.05 | 32.05 | -0.00 | PASS |
| FD001 | RMSE After | 29.31 | 29.31 | +0.00 | PASS |
| FD002 | RMSE Before | 42.40 | 42.40 | -0.00 | PASS |
| FD002 | RMSE After | 38.60 | 38.60 | -0.00 | PASS |
| FD003 | RMSE Before | 28.87 | 28.87 | +0.00 | PASS |
| FD003 | RMSE After | 26.70 | 26.70 | -0.00 | PASS |
| FD004 | RMSE Before | 42.03 | 42.03 | +0.00 | PASS |
| FD004 | RMSE After | 36.00 | 36.00 | +0.00 | PASS |

Runtime: 0.5 seconds (reads pre-generated CSVs).

---

## GÖREV 4 — IF Values Source in Comparison Tables
**Files:** `14_if_vs_ae_comparison.py`, `14b_if_vs_ae_optimal.py`

**Result: IF values are computed LIVE — no static CSV.**

Both scripts call `clust_mod.run_cluster_pipeline(subset)` at runtime (lines 47 and 53 respectively). The IF rows in `tablo_if_vs_ae.csv` and `tablo_if_vs_ae_optimal.csv` are always freshly computed. No code change required.

---

## GÖREV 5 — Seed and Library Versions
**Config:** `src/cmapss/config.py` → `SEED = 42` ✓

**requirements.txt:**
```
scikit-learn == 1.8.0
pandas       == 2.3.3
numpy        == 2.3.4
scipy        == 1.17.1
shap         == 0.51.0
matplotlib   == 3.10.9
torch        >= 2.1
pyspark      == 3.5.0
```

All results in this audit were produced with these pinned versions. The `torch>=2.1` constraint is unpinned — current install is 2.11.0 (CPU-only). For strict reproducibility, consider pinning `torch==2.11.0`.

---

## Bug Fixed During This Audit

**File:** `src/cmapss/06_shap_analysis.py`  
**Problem:** After folder restructure to `src/cmapss/`, the line `_SRC = ROOT / "src"` resolved to `src/src/` (non-existent), causing all module loads to fail.  
**Fix:** Changed to `_SRC = Path(__file__).parent` (= `src/cmapss/`).  
**Verified:** Script now runs to completion and reproduces all expected SHAP values.

---

## Conclusion

All thesis values in Tables 4.8, 4.6, and 4.13 reproduce exactly from the current codebase (deviations are floating-point rounding noise, <<0.1 cycle). The pipeline is ready for FEMTO Phase.
